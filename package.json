{
  "name": "testapi",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "npm run build && node server.js",
    "setup": "pm2 start --name mk-testapi npm -- start",
    "deploy": "git pull --no-rebase && npm i && npm run build && pm2 restart mk-testapi --update-env --time && pm2 save",
    "build": "babel server.js --out-dir ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemon": "^3.1.10"
  }
}
